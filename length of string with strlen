#include<stdio.h>
#include<string.h>
int main()
{
    char str[20];
    printf("enter a string");
    scanf("%s",str);
   int n = strlen(str);
      printf("length of string: %d",n);
      return 0;
}
