#include<stdio.h>
int main()
{
    int age;
    printf("enter an age");
    scanf("%d",&age);
    if(age>=0 && age<=12)
{
    printf("the person is a child");
    }
    else if(age>12 && age<=17)
{
    printf("the person is a teen ");
}    
    else if (age>17 && age<=60)
{
    printf("the person is an adult");
}
   else if (age>60)
{
    printf("the person is a senior citizen");
  }
   return 0;
}
