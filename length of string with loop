#include<stdio.h>
#include<string.h>
int main()
{
    char str[20];
    int i,len;
    printf("enter a string:");
    scanf("%s",str);
    i=0;
    while(str[i]!='\0')
{
    i++;
    }
    len=i; 
    printf("length of string: %d",len);
      return 0;
}
